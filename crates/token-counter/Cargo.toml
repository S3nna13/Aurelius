[package]
name = "aurelius-token-counter"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "3", features = ["napi9"] }
napi-derive = "3"
unicode-segmentation = "1.12"

[build-dependencies]
napi-build = "2"
