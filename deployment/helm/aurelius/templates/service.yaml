apiVersion: v1
kind: Service
metadata:
  name: {{ include "aurelius.fullname" . }}
  labels:
    {{- include "aurelius.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aurelius.selectorLabels" . | nindent 4 }}
